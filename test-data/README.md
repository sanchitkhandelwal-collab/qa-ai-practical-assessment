# Test Data

Use generated data for registration and the documented public demo account for login flows.

Do not commit:
- real personal data
- passwords other than public demo credentials explicitly required by the assessment
- access tokens
- API keys

For local secrets, use `.env` and keep it ignored by Git.
